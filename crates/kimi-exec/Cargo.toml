[package]
name = "kimi-exec"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Kimi Code non-interactive execution — -p/print over the host protocol (layer 4 of the Rust-first migration)"

[dependencies]
kimi-protocol = { path = "../kimi-protocol" }
kimi-server = { path = "../kimi-server" }
kimi-server-client = { path = "../kimi-server-client" }
anyhow = "1"
serde_json = { workspace = true }
tokio = { workspace = true }
