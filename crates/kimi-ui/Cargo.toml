[package]
name = "kimi-ui"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Kimi Code UI primitives — event rendering + transcript extraction shared by the CLI and the future TUI (stage D front-runner)"

[dependencies]
serde_json = { workspace = true }
tokio = { workspace = true, features = ["sync", "io-util", "rt"] }
