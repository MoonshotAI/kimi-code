---
'@moonshot-ai/kimi-code': minor
---

Customizable footer status line via `[status_line]` in `tui.toml`: pick and order the built-in slots with `items` (`mode`, `goal`, `model`, `tasks`, `cwd`, `git`, `tips`), or set `command` to render the first stdout line of a user script that receives a JSON session snapshot on stdin.
