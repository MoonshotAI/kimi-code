---
"@moonshot-ai/kimi-code-sdk": minor
---

Remove `Session.applyPersistedSecondaryModel` and the `SECONDARY_DERIVED_MODEL_ALIAS` export together with the secondary-model feature; subagent model selection is configured via `[subagent.models]` in config.toml instead.
