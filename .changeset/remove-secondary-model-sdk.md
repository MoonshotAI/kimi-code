---
"@moonshot-ai/kimi-code-sdk": minor
---

Remove the secondary-model session APIs (`Session.applyPersistedSecondaryModel` and the `SECONDARY_DERIVED_MODEL_ALIAS` export); subagent model selection is configured via `[subagent]` in config.toml instead.
