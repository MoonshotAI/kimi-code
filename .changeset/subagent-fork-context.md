---
"@moonshot-ai/kimi-code": minor
---

Add an optional `fork` parameter to the Agent tool that starts the subagent with a snapshot of the calling agent's conversation history instead of zero context.
