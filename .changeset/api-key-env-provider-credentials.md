---
"@moonshot-ai/kimi-code": patch
---

Providers can read their API key from a named environment variable (`api_key_env` in `config.toml` or the provider API), public registries can be imported without a key, and refresh or re-import now preserves hand-written provider fields and a still-valid default model.
