---
"@moonshot-ai/agent-core-v2": minor
"@moonshot-ai/kimi-code": minor
---

Support `~/.kimi-code/SYSTEM.md` to permanently replace the default main-agent system prompt for every session (v2 engine only: `KIMI_CODE_EXPERIMENTAL_FLAG=1 kimi -p`). Write the file with your prompt to use it.
