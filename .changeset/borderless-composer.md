---
"@moonshot-ai/kimi-code": patch
---

Drop the box around the input area so selecting its text with the mouse copies the text alone: the prompt now sits flush at the left edge with no trailing padding, and the input box and the /btw panel are delimited by full-width horizontal rules.
