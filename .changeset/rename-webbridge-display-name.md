---
"kimi-code": patch
---

Rename the WebBridge capability display name to "Kimi Browser Extension" in the built-in plugins card, the managed plugin manifest, the marketplace catalog, and the user docs to match the product rename. Internal ids, binary names, CDN paths, and daemon references are unchanged.
