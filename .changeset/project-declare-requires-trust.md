---
"@moonshot-ai/kimi-code": patch
---

Declaring an environment at project scope now requires the workspace to be trusted first, so a launcher declaration can no longer be written into an untrusted project and spring to life when it is trusted later.
