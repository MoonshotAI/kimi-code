---
"@moonshot-ai/kimi-code": patch
---

Upgrade the terminal UI engine to the latest upstream baseline: fixes paste-marker corruption when deleting or undoing pasted blocks, reduces keyboard input latency on Windows, keeps wide grapheme wrapping from crashing very narrow terminals, and normalizes tab characters in rendered output.
