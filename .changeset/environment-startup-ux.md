---
"@moonshot-ai/kimi-code": minor
---

Starting with `--environment` no longer freezes the interface while the remote environment connects and reports an unknown environment id immediately; subagent cards now show the environment they run in.
