---
"@moonshot-ai/agent-core": minor
"@moonshot-ai/kimi-code-sdk": minor
"@moonshot-ai/kimi-code": minor
---

Add `--add-dir`, `/add-dir`, `/dirs`, and `/remove-dir` to let users manage extra directories in the current session workspace, including directory pickers, slash-command argument completions, tool access, and TUI `@file` completions for the added roots.
