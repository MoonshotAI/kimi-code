---
"@moonshot-ai/kimi-code": patch
---

Add a `--swarm` flag to start the TUI with swarm mode enabled. Pass `kimi --swarm` to open a new session in swarm mode, or combine it with `--continue` / `--session` to resume in swarm mode.
