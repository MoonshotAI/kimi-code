---
"@moonshot-ai/kimi-code": minor
---

Add an optional `fork` parameter to the AgentSwarm tool that starts every item-spawned subagent with a snapshot of the calling agent's conversation history instead of zero context.
