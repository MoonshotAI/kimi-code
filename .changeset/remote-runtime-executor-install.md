---
"@moonshot-ai/kimi-code": minor
---

Automatically install the remote executor when an SSH or Docker runtime target does not have it yet, and print manual install or upgrade guidance when auto-install is unavailable or the executor is too old. Enable remote runtimes with `KIMI_CODE_EXPERIMENTAL_REMOTE_RUNTIME=1`.
