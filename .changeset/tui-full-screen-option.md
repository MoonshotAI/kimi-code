---
"@moonshot-ai/kimi-code": minor
---

The interactive TUI now always runs on the alternate screen, so the input box and footer stay docked at the bottom of the terminal while the conversation scrolls above them. Quitting replays the conversation into normal scrollback.
