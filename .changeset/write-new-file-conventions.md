---
"@moonshot-ai/kimi-code": minor
---

New files created by the Write tool (including files created via append) now follow the project's `.editorconfig` (`end_of_line`, `charset`) within the workspace, or the dominant line-ending/BOM style of same-extension neighboring files when no `.editorconfig` applies.
