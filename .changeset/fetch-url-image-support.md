---
"@moonshot-ai/agent-core": patch
"@moonshot-ai/kimi-server": patch
---

feat(fetch-url): support fetching images from URLs

Extend FetchURL tool to download and return image content as base64-encoded
imageData, allowing the model to view fetched images directly.
