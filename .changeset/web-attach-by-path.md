---
"@moonshot-ai/kimi-code": minor
---

Attach files, images, and videos by server-local path in the session prompt API, so the agent reads the original file in place instead of an uploaded copy; uploaded files now also appear as attachment chips in the live web transcript.
