---
"@moonshot-ai/kimi-code": patch
"@moonshot-ai/kimi-code-sdk": patch
---

Show project MCP launch targets in the workspace trust prompt, default to declining trust, and resolve fd binaries to absolute paths so untrusted workspaces cannot plant a bare-name fd executable before confirmation.
