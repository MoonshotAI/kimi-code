---
"@moonshot-ai/kimi-code": minor
---

Remind the model how much of its context budget is used and warn it once before automatic compaction so it can save state while it can still use tools; disable with `KIMI_CODE_EXPERIMENTAL_CONTEXT_BUDGET_REMINDERS=0`.
