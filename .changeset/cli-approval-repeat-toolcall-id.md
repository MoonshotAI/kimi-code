---
"@moonshot-ai/kimi-code": patch
---

Fix approval prompts no longer appearing and the session hanging from the second approval onward when using a self-hosted OpenAI-compatible endpoint that renumbers tool call ids on every response.
