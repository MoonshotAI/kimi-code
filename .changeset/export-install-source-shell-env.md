---
"@moonshot-ai/agent-core": patch
"@moonshot-ai/kimi-code-sdk": patch
"@moonshot-ai/kimi-code": patch
---

Enhance `kimi export` to include more diagnostic information in the manifest: install source (npm-global, native, etc.) and shell environment (terminal, multiplexer, shell).
