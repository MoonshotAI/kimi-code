---
"@moonshot-ai/kimi-code": minor
---

Add the experimental `--runtime <id>` flag: bind a new session to a runtime declared in the `[runtimes]` config section, taking the working directory from the entry's `defaultCwd`. Enable remote runtimes with `KIMI_CODE_EXPERIMENTAL_REMOTE_RUNTIME=1`.
