---
"@moonshot-ai/kimi-code": patch
---

Refreshing provider model lists now preserves hand-written provider fields such as `custom_headers`, and re-importing a registry no longer drops a still-valid default model.
