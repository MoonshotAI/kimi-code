---
"@moonshot-ai/kimi-code": patch
---

Refreshing provider model lists preserves hand-written provider fields such as `custom_headers`, and re-importing a registry now removes vanished providers without dropping a still-valid default model.
