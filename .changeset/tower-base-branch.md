---
"@moonshot-ai/kimi-code": patch
---

Support choosing the local base branch that tower missions fork from and merge into, instead of always using the checked-out branch (experimental; enable tower mode with KIMI_CODE_EXPERIMENTAL_TOWER=1).
