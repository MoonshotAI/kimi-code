---
"@moonshot-ai/kimi-code": patch
---

Fix parallel environment switches resolving to a nondeterministic environment, new sessions reusing a stale environment after a failed environment replacement, and local hooks and session-local MCP servers failing in remote-bound sessions.
