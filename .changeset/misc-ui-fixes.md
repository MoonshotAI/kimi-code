---
"@moonshot-ai/kimi-code": patch
---

web: Fix tooltips popping up spuriously when windows appear or regain focus, accept quoted paths when adding a workspace, show a clear error state when a UI component fails to render, and fix model provider form rows showing misplaced content after edits.
