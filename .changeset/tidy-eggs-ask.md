---
"@moonshot-ai/kimi-code": patch
---

Fire `PermissionRequest` and `PermissionResult` hooks while `AskUserQuestion` waits for and receives an answer, so hook-based status integrations can tell the agent is waiting for input.
