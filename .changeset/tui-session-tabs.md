---
"@moonshot-ai/kimi-code": minor
---

Add experimental session tabs to the TUI, enabled with `KIMI_CODE_EXPERIMENTAL_TUI_TABS=1`. Run `/new tab` to open a session in a new tab and `/tab` to switch between or close tabs.
