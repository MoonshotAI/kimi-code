---
"@moonshot-ai/kimi-code": patch
---

Persist a picked thinking effort as the default unless it exceeds the model's default effort, so the top level (such as max) can now be saved when it is the model's default.
