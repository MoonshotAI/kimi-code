---
"@moonshot-ai/kimi-code": minor
---

Add a TUI mode setting to /settings with an experimental fullscreen layout (`tui_mode = "fullscreen"` in `~/.kimi-code/tui.toml`); switching modes takes effect after restarting Kimi Code.
