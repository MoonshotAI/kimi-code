---
"@moonshot-ai/kimi-code": patch
---

Clear the screen fully when starting a new session via /new, /clear, or a session switch, instead of leaving the previous session's text above the welcome banner.
