---
"@moonshot-ai/kimi-code": patch
---

Restore pre-2.1.0 behavior: file tools can again reach symlink targets outside the workspace, background git commands honor the repository's own git configuration, and project-local `additional_dir` entries always apply without extra approval.
