---
"@moonshot-ai/kimi-code": patch
---

Keep the session list, `kimi --resume`, and `kimi --continue` fast and available while the full-text search index is still building, unavailable, or corrupt; session listings are served from their own persistent index.
