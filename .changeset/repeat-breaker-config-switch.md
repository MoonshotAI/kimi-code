---
"@moonshot-ai/kimi-code": patch
---

Add a `repeat_breaker` switch to turn off repeat reminders and forced stops. Set `repeat_breaker = false` in `config.toml` or `KIMI_CODE_REPEAT_BREAKER=0` in the environment.
