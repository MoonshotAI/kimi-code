---
"@moonshot-ai/kimi-code": patch
---

Share one remote connection across workspaces bound to the same target instead of connecting per workspace, and stop idle-reaping a connection while another workspace is still using it.
