---
"@moonshot-ai/kimi-code": patch
---

Merge the task detail panel's two identical copy icons into a single button that opens a labelled menu (copy command / copy output / copy all) with full keyboard navigation; "copy all" copies the command and output together.
