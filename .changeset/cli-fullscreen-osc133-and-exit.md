---
"@moonshot-ai/kimi-code": patch
---

Fullscreen refinements: user and assistant messages now carry OSC 133 zone markers, so Ctrl-Shift-Up/Down jumps between prompts in the transcript viewport; exiting fullscreen replays the transcript into native scrollback in the regular inline layout; the terminal progress indicator is restored when returning from the external editor mid-turn.
