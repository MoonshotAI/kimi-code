---
"@moonshot-ai/kimi-code": minor
---

Add experimental multi-stage flow runs (enable with `KIMI_CODE_EXPERIMENTAL_FLOW=true`): define staged workflows with gated transitions under `.kimi-code/flows/` and run them with `/flow:<id>`.
