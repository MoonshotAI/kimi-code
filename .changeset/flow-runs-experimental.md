---
"@moonshot-ai/kimi-code": minor
---

Add experimental multi-stage flow runs: define staged workflows with gated transitions under `.kimi-code/flows/` and run them with /flow. Enable with `KIMI_CODE_EXPERIMENTAL_FLOW=true`.
