---
"@moonshot-ai/kimi-code": minor
"@moonshot-ai/kimi-code-sdk": minor
---

Add experimental multi-stage flow runs (enable with `KIMI_CODE_EXPERIMENTAL_FLOW=true`): define staged workflows with gated transitions under the project's `.kimi-code/flows/` or the user-level `~/.kimi-code/flows/` and run them with `/flow:<id>`.
