---
"@moonshot-ai/kimi-code": minor
---

Add REST endpoints to create, edit, and delete providers, browse the models.dev directory, and import directory entries or private api.json registries, so API clients can manage providers without editing config.toml by hand.
