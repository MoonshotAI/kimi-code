---
"@moonshot-ai/kimi-code": patch
---

Show an idle-reaped remote environment as a neutral `pending` state in the footer instead of an error; only a real connection failure shows as `disconnected`.
