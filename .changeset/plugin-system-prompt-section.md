---
"@moonshot-ai/kimi-code": patch
---

Add a `systemPrompt` field to the plugin manifest so plugins can contribute instructions to the agent's system prompt. Set `systemPrompt` in `kimi.plugin.json`.
