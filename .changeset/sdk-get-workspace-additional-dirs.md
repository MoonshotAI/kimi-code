---
"@moonshot-ai/kimi-code-sdk": minor
---

Add `getWorkspaceAdditionalDirs(workDir)` to `KimiHarness` and `SDKRpcClientBase`, returning a workspace's persisted plus session-merged additional directories without creating a session.
