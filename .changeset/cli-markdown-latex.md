---
"@moonshot-ai/kimi-code": patch
---

Render LaTeX math expressions in Markdown messages as Unicode text, matching the upstream pi-tui default. Set `render_latex = false` in ~/.kimi-code/tui.toml to keep the raw source.
