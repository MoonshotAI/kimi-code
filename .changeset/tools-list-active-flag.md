---
"@moonshot-ai/kap-server": minor
"@moonshot-ai/protocol": minor
"@moonshot-ai/kimi-code": patch
---

Add an `active` flag to each tool in the server's tool listing API, reflecting the profile, global, and per-session tool gates (v2 engine only).
