---
"@moonshot-ai/kimi-code": patch
---

web: Change the default thinking setting from an on/off toggle to a level picker that can pick a concrete level (e.g. low/high/max) when the default model supports thinking levels.
