---
"@moonshot-ai/agent-core-v2": minor
"@moonshot-ai/kimi-code": minor
---

Let custom agent files restrict which sub-agent types they may delegate to with a `subagents` frontmatter allowlist (v2 engine only). Set `subagents: explore, coder` in the agent file to try it.
