---
"@moonshot-ai/kimi-code": minor
"@moonshot-ai/kimi-code-sdk": minor
---

Add `/export-md` slash command to export the current session as a human-readable Markdown file. Also exposes `Session.getContext()` on the SDK.
