---
"@moonshot-ai/klient": minor
---

Add `agent(id).getFileChanges({ turnId })` and `agent(id).getFileContentAt({ turnId, path })`, reading the daemon's experimental turn-level file snapshots over the wire.
