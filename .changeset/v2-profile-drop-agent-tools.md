---
"@moonshot-ai/kimi-code": patch
---

Remove the Agent and AgentSwarm tools from the built-in agent profiles, so sessions no longer expose subagent delegation by default. Custom profiles that list these tools explicitly can still opt in.
