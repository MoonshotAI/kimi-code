---
"@moonshot-ai/kimi-code": minor
---

Remote-bound sessions resumed after a restart now build their workspace instructions, skills, and profiles on the persisted remote environment instead of the local carrier, and subagent prompt context, file history, and sessionless file suggestions now resolve against the subagent's bound environment.
