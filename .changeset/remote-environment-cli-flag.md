---
"@moonshot-ai/kimi-code": minor
---

Add the experimental `--environment <id>` flag: bind a new session to an environment declared in the `[environments]` config section, taking the working directory from the entry's `defaultCwd`. Enable remote environments with `KIMI_CODE_EXPERIMENTAL_REMOTE_RUNTIME=1`.
