---
"@moonshot-ai/kimi-code": minor
---

Add experimental OS keychain credential storage. Set KIMI_CODE_EXPERIMENTAL_KEYRING=1 to opt in (KIMI_DISABLE_KEYRING=1 to opt out); the plaintext file store remains the fallback when the keychain is unavailable.
