---
"@moonshot-ai/kimi-code": minor
"@moonshot-ai/kimi-code-sdk": minor
---

Add inline skill selection throughout prompts and atomic skill-prompt submission to SDK sessions. Type `/` after whitespace to select a skill, or use `session.promptWithSkills(...)` to submit skills with a prompt.
