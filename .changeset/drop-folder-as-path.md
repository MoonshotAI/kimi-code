---
"@moonshot-ai/kimi-code": patch
---

web: Dropping a folder onto the composer is now ignored instead of failing the upload. (Inserting the dropped folder's path is desktop-only — browsers do not expose absolute file paths.)
