The current goal is budget-limited (its budget was exhausted). The goal is not automatically continuing.

<goal>
<objective>${objective}</objective>
${completion_criterion_block}
<status>${status}</status>
<progress>${progress}</progress>
</goal>

The budget has been exhausted. You can:
1. Summarize what was accomplished and what remains.
2. If the user wants to continue, they can set a new budget via SetGoalBudget or create a new goal.