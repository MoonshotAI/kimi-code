export default {
  /** Shared title of the right-side panel — both occupants are previews. */
  preview: 'Preview',
  /** Generic confirm / cancel button labels (used by ConfirmDialog). */
  confirm: 'Confirm',
  cancel: 'Cancel',
  /** Error message shown when an async component fails to load. */
  loadErrorTitle: 'Failed to load component',
  /** Button label to retry loading a failed async component. */
  loadErrorRetry: 'Reload',
} as const;
