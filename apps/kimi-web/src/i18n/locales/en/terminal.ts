export default {
  /** Label shown when a terminal process has exited. */
  exited: 'exited',
  /** Fit terminal to container size. */
  fit: 'Fit',
  /** Start a new terminal session. */
  new: 'New',
  /** Message shown while the terminal is starting up. */
  starting: 'starting terminal…',
} as const;