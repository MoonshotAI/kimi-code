<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron/V8 needs JIT pages. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>

  <!-- V8 writes to executable memory during codegen. -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- The in-process server dlopen's node-pty (pty.node) at runtime; it is a
       third-party native module and not signed by our Team. Without this the
       hardened runtime rejects it. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Some users set NODE_OPTIONS / DYLD_* envs; allow them. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
