# Model Adaptation: Example Template
#
# This file is auto-generated by `prompt-optimizer probe`.
# It contains prompt patches to inject for specific model weaknesses.
# The rendering pipeline loads this when the active model matches.
#
# Format: Each section below is injected AFTER the corresponding
# section in system.md when this model is active.

## Negation Compliance Patch

When instructing yourself not to do something, reframe as a positive:
- Instead of "do NOT use emoji" → "use plain text without decorative characters"
- Instead of "do NOT run git commit" → "leave commits to the user"

## Long-paragraph Memory Patch

Critical rules are numbered for reliable recall:
1. No git mutations without explicit request
2. Use dedicated tools (Read/Glob/Grep) before Bash
3. Reply in user's language
4. Minimal changes only
