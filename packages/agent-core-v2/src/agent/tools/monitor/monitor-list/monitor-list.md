List the monitors registered by this agent.

Shows every monitor with its id, type, status (`active`, `fired`, `cancelled`, `ended`, `lost`), pattern/command/path, and trigger for fired monitors. Monitors that survived a session restart appear as `lost` and never fire again.
