---
name: debugging
description: |
  Debugging expertise bundle covering systematic debugging and advanced root cause analysis.
  Use when debugging errors, troubleshooting bugs, investigating crashes, or analyzing stack traces.
when-to-use: "Load BEFORE investigating errors, stack traces, crashes, or flaky failures."
---

# Debugging

This is a parent skill bundle containing sub-skills for debugging and troubleshooting.

## Sub-skills

- `debugging.debugger` — Systematic debugging and root cause analysis
- `debugging.debugging-wizard` — Stack trace parsing, log correlation, hypothesis-driven debugging

Load a sub-skill with the `Skill` tool using its dotted name, e.g. `Skill(skill="debugging.debugger")`.
