Abort the active flow run.

Use only when the user asks to stop the run, or when a stage's premise is invalidated and the user directs a restart. The abort is recorded in the run journal; the run cannot be resumed afterwards — starting over means a new FlowStart.
