### 新功能

- 查看图片或视频的工具结果现在支持点击全屏预览，图片可缩放。
- 按工作区分组的会话视图新增「按最近活动」排序选项。

### 优化

- 降低长时间运行的内存与 CPU 占用，减少长会话卡顿。
- 为没有文字说明的图标按钮补充悬停提示。
- 会话列表的 PR 标记改为带背景的小标签样式。
- 优化侧边栏会话状态和排序。
- 加宽侧边栏可拖拽的最小宽度。

### 修复

- 修复会话内新建 PR 后，列表上的 PR 标记不刷新的问题。
