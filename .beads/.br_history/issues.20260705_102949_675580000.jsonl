{"id":"kimigram-75s","title":"Epic: Kimigram — Telegram bridge for kimi-code","design":"## Requirements (IMMUTABLE)\n- R1: A standalone TypeScript/Node.js sidecar service runs alongside a running kimi-code server instance.\n- R2: The sidecar receives text messages from Telegram via long polling and forwards them as user prompts to a linked kimi-code session using the REST API (`POST /api/v1/sessions/{id}/prompts`).\n- R3: The sidecar subscribes to the kimi-code WebSocket event stream (`/api/v1/ws`) and sends Telegram notifications for turn completion, task completion, goal completion, and human approval requests.\n- R4: A Telegram user can only interact after pairing via a one-time code generated from inside an authenticated kimi-code session (`/telegram pair` or CLI equivalent).\n- R5: All outgoing assistant text is converted from Markdown to Telegram-compatible MarkdownV2, with automatic fallback to plain text if conversion would produce invalid markup.\n- R6: Reply threading is preserved: Telegram replies use `reply_parameters` linked to the original bot message, and incoming replies map back to the correct kimi-code `user_message_id`.\n- R7: The sidecar auto-reconnects to both Telegram and kimi-code on network failures and deduplicates incoming Telegram updates by `update_id`.\n\n## Success Criteria (MUST ALL BE TRUE)\n- [ ] User can pair a Telegram chat with a running kimi-code session using `/start <one-time-code>`.\n- [ ] Unpaired users receive setup instructions and cannot submit prompts.\n- [ ] Text messages from Telegram appear as user prompts in the linked kimi-code session.\n- [ ] Turn/task/goal completion and approval-request events in kimi-code result in Telegram notifications.\n- [ ] Reply threading works in both directions without message collisions.\n- [ ] Markdown-formatted assistant responses render correctly in Telegram without Bot API rejections.\n- [ ] Integration tests pass against mocked Telegram and mocked kimi-code servers.\n- [ ] All unit tests passing.\n\n## Anti-Patterns (FORBIDDEN)\n- ❌ NO passing raw kimi-code Markdown directly to Telegram `parse_mode=MarkdownV2` (reliability: headings, lists, tables, and unescaped punctuation cause message rejections).\n- ❌ NO storing the Telegram bot token or kimi-code bearer token in source code or committed config (security: use environment variables or a secrets manager).\n- ❌ NO trusting Telegram `from.id` or `chat.id` alone for sensitive actions (security: pairing must be authorized by a code generated inside the authenticated kimi-code session).\n- ❌ NO building the bot into kimi-code core or modifying upstream packages (scope: standalone sidecar per user decision).\n- ❌ NO creating a full task tree upfront (process: tasks created iteratively by executing-plans as we learn).\n- ❌ NO relying only on unit tests with mocked boundaries (validation: integration tests with real HTTP/WebSocket behavior are required for a network bridge).\n- ❌ NO using webhooks that require a public HTTPS endpoint by default (simplicity: start with long polling; webhooks are an opt-in future enhancement).\n\n## Approach\nBuild a single TypeScript/Node.js sidecar service that bridges Telegram and kimi-code. The bot layer uses grammY with long polling to receive Telegram updates and send messages. The kimi-code integration layer uses the existing REST and WebSocket APIs exposed by `packages/server`. A SQLite store persists pairing records and message-thread mappings. A dedicated formatter module converts kimi-code's Markdown output to Telegram MarkdownV2 with plain-text fallback. Pairing is bootstrapped by a one-time code generated inside an authenticated kimi-code session, ensuring only authorized users can link Telegram to their session.\n\n## Architecture\n- `src/bot.ts` — grammY bot setup, command handlers (`/start`), incoming message handler, notification sender.\n- `src/pairing.ts` — one-time code generation, validation, expiry, pairing storage.\n- `src/kimi/client.ts` — REST client for prompt submission and session operations.\n- `src/kimi/ws.ts` — WebSocket client for event subscription, reconnect, and event dispatch.\n- `src/threads.ts` — maps Telegram `message_id` ↔ kimi-code `user_message_id` for reply threading.\n- `src/formatting/telegramMarkdown.ts` — Markdown → Telegram MarkdownV2 converter with plain-text fallback.\n- `src/store.ts` — SQLite persistence layer for pairings and thread mappings.\n- `src/config.ts` — env-based configuration (bot token, kimi server URL, token path, etc.).\n- `src/index.ts` — service bootstrap and lifecycle.\n\n## Design Rationale\n\n### Problem\nkimi-code runs as a local agent with a terminal UI and a browser UI, but neither surfaces push notifications or allows asynchronous mobile input. Users who step away from their desk miss completion events and cannot quickly send follow-up prompts from their phone. A Telegram bot bridges this gap by providing mobile push notifications and a two-way messaging channel.\n\n### Research Findings\n**Codebase:**\n- `packages/protocol/src/events.ts` — assistant text is streamed as `assistant.delta` events (`delta: string`); final messages are stored as `text` content.\n- `packages/protocol/src/message.ts` — `MessageContent` includes `text`, `tool_use`, `tool_result`, `image`, `video`, `file`, and `thinking`.\n- `packages/server/src/routes/prompts.ts` — prompts are submitted via `POST /api/v1/sessions/{id}/prompts` with bearer auth.\n- `packages/server/src/services/gateway/wsGateway.ts` — WebSocket at `/api/v1/ws` uses bearer subprotocol auth.\n- `packages/agent-core/src/config/schema.ts` — existing hook system supports `Notification`/`SessionEnd` events, but is one-way and less flexible than WS events.\n- `apps/kimi-web/src/api/daemon/ws.ts` and `client.ts` — browser UI consumes the same REST/WS surface we will use.\n\n**External:**\n- Telegram Bot API uses HTTPS calls to `api.telegram.org`; grammY is the recommended TypeScript-first framework.\n- Long polling is best for local/sidecar deployments; webhooks require public HTTPS endpoints.\n- Telegram MarkdownV2 is strict: no headings, lists, tables, or images; many characters must be escaped.\n- `telegramify-markdown` and similar libraries exist, but a small custom converter keeps dependencies minimal and testable.\n\n### Approaches Considered\n\n#### 1. Cohesive TypeScript sidecar using grammY + WS + REST ✓\n\n**What it is:** A single Node.js service that uses grammY for Telegram long polling, connects to kimi-code's WebSocket for events, and calls the REST API to submit prompts.\n\n**Investigation:**\n- Confirmed kimi-code exposes `/api/v1/sessions/{id}/prompts` and `/api/v1/ws` with bearer auth.\n- Confirmed grammY supports long polling out of the box and has excellent TypeScript types.\n- Confirmed reply threading is possible via Telegram `reply_parameters` and message-ID mapping.\n\n**Pros:**\n- Matches kimi-code's TypeScript/Node.js stack.\n- Single service is easier to operate and reason about.\n- Real-time WS events provide richer notifications than hooks.\n- Long polling avoids public URL requirement, fitting local-first usage.\n\n**Cons:**\n- Sidecar must be running continuously.\n\n**Chosen because:** It is the most cohesive, matches the codebase stack, and avoids public-infrastructure complexity.\n\n#### 2. Hooks for notifications + separate bot for input ❌\n\n**What it is:** Use kimi-code's `[[hooks]]` config to call a notification script, and run a separate small bot service only to receive Telegram messages.\n\n**Why we looked at this:** It seemed to reuse built-in mechanisms for the notification direction.\n\n**Investigation:**\n- Hook commands receive JSON on stdin and can call Telegram API.\n- Hook events include `Notification` and `SessionEnd` but do not cover all milestone events uniformly.\n\n**Pros:**\n- Reuses existing hook infrastructure for notifications.\n\n**Cons:**\n- Two moving parts with shared state (pairing/threads) required.\n- Hook payload is less structured than WS events.\n- Harder to maintain reply-thread context across two processes.\n\n**⚠️ REJECTED BECAUSE:** Split responsibility increases operational complexity and makes reply threading harder, with no clear benefit over the cohesive sidecar.\n\n**🚫 DO NOT REVISIT UNLESS:** The cohesive sidecar proves too resource-heavy or upstream disables WebSocket access.\n\n#### 3. Cloud/webhook relay ❌\n\n**What it is:** Run the Telegram bot as a public cloud function or VPS with webhooks; relay messages to the local kimi-code instance via a tunnel or direct REST.\n\n**Why we looked at this:** Could enable mobile access when the local machine is not reachable.\n\n**Investigation:**\n- Webhooks require a public HTTPS endpoint and valid TLS certificate.\n- kimi-code server binds loopback by default (`127.0.0.1:58627`).\n\n**Pros:**\n- Enables remote/mobile usage without direct machine access.\n\n**Cons:**\n- Adds public-infrastructure and secret-management burden.\n- Conflicts with kimi-code's local-first security model.\n- Overkill for the primary use case.\n\n**⚠️ REJECTED BECAUSE:** It introduces external dependencies and public exposure that contradict the local-first design; long polling is sufficient for the sidecar model.\n\n**🚫 DO NOT REVISIT UNLESS:** There is an explicit requirement for remote access without running anything on the user's machine.\n\n### Scope Boundaries\n**In scope:**\n- Standalone TypeScript sidecar service.\n- Telegram long-polling bot with `/start <code>` pairing.\n- Forwarding Telegram text messages to kimi-code prompts.\n- Sending Telegram notifications for turn/task/goal completion and approval requests.\n- Markdown → Telegram MarkdownV2 conversion with plain-text fallback.\n- Reply-thread preservation via message-ID mapping.\n- Auto-reconnect and basic rate-limit handling.\n- Unit and integration tests.\n\n**Out of scope (deferred/never):**\n- Webhook mode (deferred; may be added later as opt-in).\n- Multi-provider support (Slack, WhatsApp, etc.) — never in this epic.\n- Voice/image/file messages from Telegram — deferred.\n- Multiple active sessions per Telegram chat — deferred; MVP supports one linked session per chat.\n- Hosting/cloud deployment guides — deferred.\n\n### Open Questions\n- Should the sidecar auto-create a new kimi-code session if none exists, or should it always require an active session?\n- Should completion notifications include the full assistant response, a truncated preview, or just a \"done\" notice with a command to fetch the full text?\n- Should failed pairing attempts be rate-limited or logged for abuse detection?\n\n## Design Discovery (Reference Context)\n\n### Key Decisions Made\n\n| Question | User Answer | Implication |\n|----------|-------------|-------------|\n| Who will use this bot? | Anyone who can authenticate | Requires explicit pairing flow; cannot trust Telegram identity alone. |\n| What events should notify? | Task completion + important milestones | Listen for `turn.ended`, `event.task.completed`, `background.task.terminated`, `goal.updated`, and `event.approval.requested`. |\n| Where should the bot live? | Separate small service/sidecar | Build a standalone repo; integrate via public REST/WS APIs only. |\n| How should users pair? | One-time pairing code in kimi-code UI/CLI | Generate single-use, expiring codes authorized from inside the authenticated session. |\n| What stack? | Match kimi-code stack (TypeScript/Node.js) | Use grammY for Telegram, `ws` for WebSocket, native `fetch` for REST. |\n| Preserve reply threads? | Yes | Store Telegram `message_id` ↔ kimi-code `user_message_id` mappings. |\n| Include Markdown conversion? | Yes | Core requirement; never send raw Markdown to Telegram. |\n| Standalone repo or inside monorepo? | Standalone repo | This project is the kimigram sidecar, not an upstream package. |\n\n### Research Deep-Dives\n\n#### kimi-code Integration Surface\n**Question explored:** How does an external system push prompts and receive events from a running kimi-code instance?\n**Sources consulted:**\n- `packages/server/src/routes/prompts.ts` — prompt submission route.\n- `packages/server/src/services/gateway/wsGateway.ts` — WebSocket gateway and auth.\n- `packages/protocol/src/events.ts` — event types and shapes.\n- `packages/protocol/src/message.ts` — message content schema.\n- `packages/agent-core/src/config/schema.ts` — hook system.\n\n**Findings:**\n- REST API supports prompt submission under `/api/v1/sessions/{id}/prompts`.\n- WS API at `/api/v1/ws` supports subscription and replay via cursors.\n- Hook system exists but is one-way and less structured.\n\n**Conclusion:** Use REST for prompt submission and WS for real-time notifications.\n\n#### Telegram Bot API and Patterns\n**Question explored:** What is the simplest, reliable way to receive Telegram messages and send notifications?\n**Sources consulted:**\n- Telegram Bot API docs (core.telegram.org/bots/api).\n- grammY deployment guide (grammy.dev/guide/deployment-types).\n- grammY flood/retry guide (grammy.dev/advanced/flood).\n\n**Findings:**\n- Long polling is best for local/sidecar deployments.\n- grammY is the modern TypeScript-first framework.\n- MarkdownV2 requires strict escaping and does not support many common Markdown features.\n\n**Conclusion:** Use grammY with long polling and implement a Markdown-to-TelegramV2 converter.\n\n### Dead-End Paths\n\n#### Hook-only notifications\n**Why explored:** It seemed natural to reuse kimi-code's existing `[[hooks]]` configuration.\n**Investigation:**\n- Hooks can call external commands on `Notification` and `SessionEnd` events.\n- Hook payload is less structured than WS events and does not cover all milestones uniformly.\n\n**Why abandoned:** One-way nature and inconsistent event coverage make it inferior to WebSocket events for a two-way bridge.\n\n#### Webhook mode by default\n**Why explored:** Webhooks are often recommended for production bots.\n**Investigation:**\n- Requires public HTTPS endpoint and TLS certificate.\n- Conflicts with kimi-code's local-first loopback-only server.\n\n**Why abandoned:** Overkill for the local sidecar model; long polling is sufficient and simpler.\n\n### Open Concerns Raised\n\n- \"Will kimi-code responses render correctly in Telegram Markdown?\" → Confirmed they will not without conversion; addressed by making Markdown→TelegramV2 conversion a core requirement.\n- \"What if the user blocks the bot?\" → Handle `my_chat_member` updates and mark pairing inactive.","status":"open","priority":1,"issue_type":"epic","owner":"dmitry.polishuk@gmail.com","created_at":"2026-07-05T08:30:48.404641Z","created_by":"Dmitry Polishuk","updated_at":"2026-07-05T08:30:48.404641Z","source_repo":".","compaction_level":0,"labels":["kimi-code","sidecar","telegram"]}
{"id":"kimigram-75s.1","title":"Task 1: Bootstrap kimigram and implement Telegram pairing flow","design":"## Goal\nBootstrap the kimigram TypeScript project and implement the Telegram pairing flow (`/start <code>`). After this task, the sidecar can be started, receive a Telegram `/start` command, validate a one-time pairing code against a SQLite store, and confirm or reject the pairing.\n\n## Effort Estimate\n6–10 hours.\n\n## Implementation\n\n1. **Project bootstrap**\n   - Initialize `package.json` with TypeScript, Vitest, `tsx`, `grammy`, `ws`, `better-sqlite3` (or `libsql`/sqlite wrapper), and Zod.\n   - Add `tsconfig.json`, `.env.example`, `.gitignore`.\n   - Add npm scripts: `dev`, `build`, `test`, `lint`.\n   - Add a basic `README.md` with setup instructions.\n\n2. **Configuration** (`src/config.ts`)\n   - Load `TELEGRAM_BOT_TOKEN` (required).\n   - Load `KIMI_SERVER_URL` (default `http://127.0.0.1:58627`).\n   - Load `KIMI_TOKEN_PATH` (default `~/.kimi-code/server/token`).\n   - Load `DATABASE_PATH` (default `./data/kimigram.db`).\n   - Load `PAIRING_CODE_TTL_MINUTES` (default `10`).\n   - Validate with Zod; fail fast on startup if required env vars are missing.\n\n3. **SQLite store** (`src/store.ts`)\n   - Schema `pairings`:\n     - `id` TEXT PRIMARY KEY\n     - `session_id` TEXT NOT NULL\n     - `telegram_chat_id` BIGINT NOT NULL UNIQUE\n     - `telegram_user_id` BIGINT\n     - `pairing_code` TEXT UNIQUE\n     - `code_expires_at` DATETIME\n     - `paired_at` DATETIME\n     - `active` BOOLEAN DEFAULT 1\n   - Methods:\n     - `createPairingRequest(sessionId, code, expiresAt)`\n     - `activatePairing(code, chatId, userId)`\n     - `getPairingByChatId(chatId)`\n     - `getPairingBySessionId(sessionId)`\n     - `deactivatePairing(chatId)`\n\n4. **Pairing service** (`src/pairing.ts`)\n   - `generatePairingCode(sessionId: string): string` — creates a random 6-character alphanumeric code, stores a pending pairing with expiry.\n   - `validatePairingCode(code: string): PairingRequest | null` — returns the pending session if code exists and not expired; otherwise null.\n\n5. **Telegram bot handler** (`src/bot.ts`)\n   - Initialize grammY bot with `Bot` from token.\n   - Handle `/start <code>`:\n     - Trim and uppercase code.\n     - Call `validatePairingCode`.\n     - If valid: store `chat_id`/`user_id`, mark active, reply \"Paired with session `<id>`. You can now send messages here.\"\n     - If invalid/expired: reply \"Invalid or expired code. Generate a new one from your kimi-code session.\"\n   - Handle `/start` without code: reply with setup instructions.\n\n6. **Service entrypoint** (`src/index.ts`)\n   - Load config.\n   - Initialize store.\n   - Start grammY bot in long-polling mode (`bot.start()`).\n   - Graceful shutdown on SIGINT/SIGTERM.\n\n7. **Tests**\n   - Unit tests for `pairing.ts`:\n     - `generate_pairing_code_creates_unique_codes` — catches collisions/buggy RNG.\n     - `validate_pairing_code_returns_session_for_valid_code` — happy path.\n     - `validate_pairing_code_returns_null_after_expiry` — catches TTL bugs.\n     - `validate_pairing_code_is_case_insensitive` — catches case normalization bugs.\n   - Unit tests for `store.ts`:\n     - `create_pairing_request_stores_pending_code` — verifies persistence.\n     - `activate_pairing_links_chat_id_and_marks_active` — verifies activation.\n     - `activate_pairing_with_duplicate_chat_id_replaces_old_pairing` — catches uniqueness/re-pairing bugs.\n     - `get_pairing_by_chat_id_returns_null_for_unknown_chat` — catches lookup bugs.\n   - Integration test for `/start <code>` using grammY's `Bot.api` mocking or a local HTTP fake:\n     - `start_with_valid_code_replies_success_and_activates_pairing`\n     - `start_with_expired_code_replies_invalid`\n     - `start_without_code_replies_setup_instructions`\n\n## Success Criteria\n- [ ] `npm install && npm run dev` starts the service and begins Telegram long polling.\n- [ ] `/start VALIDCODE` pairs the chat and stores the mapping in SQLite.\n- [ ] `/start INVALID` replies with a clear error and does not create a pairing.\n- [ ] `/start` without a code replies with setup instructions.\n- [ ] Pairing codes expire after the configured TTL.\n- [ ] Unit and integration tests pass (`npm test`).\n- [ ] Lint/typecheck passes (`npm run lint` / `npm run build`).\n\n## Anti-patterns\n- ❌ NO hardcoded Telegram bot token or secrets in source/config files (security: env vars only).\n- ❌ NO trusting Telegram `chat.id` without a validated pairing code (security: unpaired chats cannot create pairings).\n- ❌ NO storing pairing codes plaintext without expiry (security: codes must be single-use and time-bound).\n- ❌ NO using `console.log` for operational logging (maintainability: use a structured logger like `pino`).\n- ❌ NO skipping tests for store edge cases (validation: re-pairing, expiry, and unknown lookups must be covered).\n\n## Key Considerations\n\n**Edge Case: Malformed or missing code**\n- `/start` with no argument → reply with setup instructions.\n- `/start   ` (whitespace only) → treat as missing code.\n- `/start abc!@#` (special characters) → normalize/uppercase and let validation fail cleanly.\n\n**Edge Case: Code expiry and reuse**\n- A code must be usable only once. After activation or after TTL expires, `validatePairingCode` must return null.\n- TTL must be checked against wall-clock time in UTC to avoid timezone bugs.\n- Expired codes should be cleaned up periodically or on access; at minimum they must not activate pairings.\n\n**Edge Case: Re-pairing and chat_id uniqueness**\n- `telegram_chat_id` has a UNIQUE constraint. If a chat re-pairs with a new session/code, the old pairing must be deactivated or replaced, not cause a DB error.\n- Decide and document: re-pairing overwrites the previous session mapping.\n\n**Edge Case: Concurrent /start requests**\n- SQLite with `better-sqlite3` is serialized. Wrap activation in a transaction to prevent race conditions where two chats claim the same code.\n\n**Edge Case: Database failures**\n- If the DB file directory does not exist, create it on startup.\n- If DB operations fail, the bot should reply with a generic error message and log the real error; never expose stack traces to Telegram.\n\n**Edge Case: Environment misconfiguration**\n- Missing `TELEGRAM_BOT_TOKEN` must cause a clear startup error before the bot attempts to connect to Telegram.\n- Invalid numeric config values (e.g. `PAIRING_CODE_TTL_MINUTES=abc`) must fail fast with a descriptive Zod error.\n\n**Reference Implementation**\n- Study grammY docs for command argument parsing: https://grammy.dev/guide/commands\n- Study `better-sqlite3` transactions for atomic activation.","status":"closed","priority":1,"issue_type":"feature","owner":"dmitry.polishuk@gmail.com","created_at":"2026-07-05T08:31:38.820799Z","created_by":"Dmitry Polishuk","updated_at":"2026-07-05T08:59:26.156467Z","closed_at":"2026-07-05T08:59:26.156422Z","close_reason":"done","source_repo":".","compaction_level":0,"labels":["bootstrap","pairing","telegram"],"dependencies":[{"issue_id":"kimigram-75s.1","depends_on_id":"kimigram-75s","type":"parent-child","created_at":"2026-07-05T08:31:38.822807Z","created_by":"Dmitry Polishuk","metadata":"","thread_id":""}]}
{"id":"kimigram-75s.2","title":"Task 2: Implement kimi-code REST client and forward Telegram messages","description":"## Goal\nImplement the REST client and message handler that forwards text messages from paired Telegram chats to the linked kimi-code session as user prompts.\n\n## Implementation\n1. Extend `src/config.ts` to validate:\n   - `KIMI_SERVER_URL` (default `http://127.0.0.1:58627`)\n   - `KIMI_BEARER_TOKEN` or `KIMI_TOKEN_FILE` (default `~/.kimi-code/token`)\n2. Create `src/kimi/client.ts`:\n   - `createKimiClient(config)` exposing `submitPrompt(sessionId, text)`.\n   - Use native `fetch`, set `Authorization: Bearer <token>` and `Content-Type: application/json`.\n   - Throw `KimiClientError` on non-2xx responses, including status and response body.\n   - Parse and return the prompt response (at least `id`).\n3. Update `src/bot.ts`:\n   - Add a `message:text` handler that looks up the paired session for the chat.\n   - If paired, call `client.submitPrompt(sessionId, ctx.message.text)`.\n   - If unpaired, reply with the setup instructions used by `/start`.\n4. Wire the client and bot in `src/index.ts`.\n5. Add tests:\n   - Unit tests for `createKimiClient` using mocked `fetch`.\n   - Bot integration test verifying a text message from a paired chat triggers a prompt submission.\n   - Bot integration test verifying an unpaired chat receives setup instructions and no network call is made.\n\n## Success Criteria\n- [ ] Config validates the new kimi-code connection settings.\n- [ ] `submitPrompt` calls `POST /api/v1/sessions/{id}/prompts` with the bearer token and JSON body.\n- [ ] Telegram text from a paired chat is forwarded as a user prompt.\n- [ ] Unpaired chats receive setup instructions and no prompt is sent.\n- [ ] HTTP errors are surfaced by the client.\n- [ ] All tests passing and `npm run lint` clean.","design":"## Effort Estimate\n6-8 hours\n\n## Detailed Implementation Checklist\n1. **Config** (`src/config.ts`):\n   - Add `KIMI_SERVER_URL` with default `http://127.0.0.1:58627`.\n   - Add `KIMI_BEARER_TOKEN` (optional env fallback) and `KIMI_TOKEN_FILE` with default `~/.kimi-code/token`.\n   - Resolve token at config-load time: use `KIMI_BEARER_TOKEN` if present; otherwise read file, trim whitespace/newlines. Fail fast if neither resolves.\n   - Normalize `kimiServerUrl` to strip trailing `/`.\n\n2. **REST client** (`src/kimi/client.ts`):\n   - Export `KimiClientError` class extending `Error` with `status: number` and `responseBody: string`.\n   - Export `createKimiClient(config)` returning `{ submitPrompt(sessionId, text): Promise<{ id: string }> }`.\n   - `submitPrompt` must:\n     - `POST ${kimiServerUrl}/api/v1/sessions/${encodeURIComponent(sessionId)}/prompts`.\n     - Send JSON body `{ text }`.\n     - Set `Authorization: Bearer <token>` and `Content-Type: application/json`.\n     - On 2xx, parse JSON and validate response has `id` string; return it.\n     - On non-2xx, read response text and throw `KimiClientError(status, body)`.\n   - Keep client dependency-free besides native `fetch` and `node:fs` (used only at config time).\n\n3. **Message handler** (`src/bot.ts`):\n   - Change `createBot` signature to `createBot(config, pairingService, store, kimiClient)`.\n   - Add `bot.on('message:text', createMessageHandler(pairingService, store, kimiClient))`.\n   - `createMessageHandler` must:\n     - Look up pairing by `ctx.chat.id`. If none or inactive, reply with setup instructions and return.\n     - Get trimmed `ctx.message.text`. If empty after trim, reply asking for a non-empty message and return.\n     - Call `kimiClient.submitPrompt(pairing.sessionId, text)`.\n     - On `KimiClientError`, reply with a concise error (e.g., \"Failed to send prompt: <status>\").\n     - On success, reply with a confirmation (e.g., \"Prompt sent.\").\n   - Do NOT use `ctx.from.id` to select the session; always use the chat-to-session pairing.\n\n4. **Bootstrap** (`src/index.ts`):\n   - Load config, open store, create pairing service, create kimi client, create bot, start long polling.\n   - Ensure token-loading errors fail fast before the bot starts.\n\n5. **Tests**:\n   - `src/kimi/client.test.ts`:\n     - Mock `globalThis.fetch` and assert correct URL, method, headers, and JSON body.\n     - Assert successful response returns `{ id }`.\n     - Assert 401 throws `KimiClientError` with status and body.\n     - Assert 500 throws with response body.\n     - Assert malformed JSON response throws.\n   - `src/bot.test.ts`:\n     - Add `message:text` test for paired chat: mock `kimiClient` resolves; assert `submitPrompt` called with session id and text; assert bot replies confirmation.\n     - Add `message:text` test for unpaired chat: mock client rejects if called; assert setup instructions reply.\n     - Add test for empty text: assert no client call and help reply.\n     - Add test for `KimiClientError`: assert user-facing error reply.\n\n## Key Considerations (SRE Review)\n\n**Token security**\n- Load token from env or file only. Never commit it. Fail fast at startup if missing.\n- Trim newlines from file token; token files often contain a trailing `\\n`.\n\n**Failure modes**\n- kimi-code server unreachable: `fetch` will throw a network error. The message handler should catch it and reply to the user (do not crash the bot).\n- 401/403: indicate misconfigured token; reply to user generically and log the status.\n- 404: linked session no longer exists; reply that the session is unavailable.\n- 5xx: server error; reply to retry later.\n- Empty prompt: do not forward; reply with usage hint.\n\n**Data validation**\n- Validate REST response has a string `id`. A missing or non-string id should throw a client-side error rather than propagate `undefined`.\n- Use `encodeURIComponent` on `sessionId` in the URL path.\n\n**Scope boundaries**\n- Do NOT implement WebSocket notifications here (Task 3).\n- Do NOT implement message-thread / reply mapping here (Task 4).\n- Do NOT implement Markdown conversion here (Task 5).\n\n## Anti-patterns\n- ❌ NO storing the bearer token in source code or committed config (security).\n- ❌ NO silently swallowing HTTP errors (reliability: surface status/body).\n- ❌ NO forwarding messages from unpaired chats (security).\n- ❌ NO using Telegram `from.id` to look up the session (security: use the pairing record).\n- ❌ NO sending empty or whitespace-only prompts to kimi-code (data quality).\n- ❌ NO accepting an unexpected REST response shape without validation (correctness).\n- ❌ NO TODO stubs for token loading or error handling (completeness).","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-05T09:54:07.284623Z","created_by":"dpolishuk","updated_at":"2026-07-05T10:06:06.727430Z","closed_at":"2026-07-05T10:06:06.727384Z","close_reason":"done","source_repo":"kimigram","source_repo_path":"/Users/dpolishuk/work/kimigram","compaction_level":0,"original_size":0,"labels":["kimi-code","sidecar","telegram"],"dependencies":[{"issue_id":"kimigram-75s.2","depends_on_id":"kimigram-75s","type":"parent-child","created_at":"2026-07-05T09:54:07.284623Z","created_by":"dpolishuk"}]}
